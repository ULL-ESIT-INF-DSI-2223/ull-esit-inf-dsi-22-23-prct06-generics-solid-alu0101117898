/*
import 'mocha';
import {expect} from 'chai';
import { Cancion } from '../../src/ejercicio-3/cancion';
import { Disco } from '../../src/ejercicio-3/disco';

const disco1 = new Disco("El Amor", 1991, [new Cancion("La vida misma", 240, ["pop"], false, 10000), new Cancion("Top", 200, ["indie"], false, 2000000)]);

describe('Class Discob check', () => {
  it("disco1.getNombre() returns 'El Amor' ", () => {
    expect(disco1.getNombre()).to.be.eql("El Amor");
  });

  it("disco1.getAño() returns 1991", () => {
    expect(disco1.getAño()).to.be.eql(1991);
  });

  it("disco1.getCanciones() returns disco1", () => {
    expect(disco1.getCanciones()).to.be.eql([new Cancion("La vida misma", 240, ["pop"], false, 10000), new Cancion("Top", 200, ["indie"], false, 2000000)]);
  });
});
*/